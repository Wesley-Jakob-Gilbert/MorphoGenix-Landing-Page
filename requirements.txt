# Runtime deps — pinned for reproducible builds.
# Bump via `pip-compile` or manual review. Rebuild Docker image after changes.
fastapi==0.118.3
uvicorn[standard]==0.40.0
jinja2==3.1.4
python-dotenv==1.0.1
httpx==0.27.2
pydantic[email]==2.12.0
slowapi==0.1.9

# Dev tools
ruff==0.9.0
